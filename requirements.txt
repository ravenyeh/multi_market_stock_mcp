# 多市場股票 MCP 服務器依賴套件
# Multi-Market Stock MCP Server Dependencies

# MCP 框架
mcp>=1.0.0

# HTTP 客戶端（異步請求）
httpx>=0.27.0

# 資料驗證
pydantic>=2.0.0

# Event Loop 管理（推薦安裝，解決 asyncio 衝突）
nest-asyncio>=1.5.0

# 注意：
# 本版本使用免費 API，不需要額外安裝 yfinance 或 akshare
# 所有市場查詢都通過 HTTP 直接呼叫公開 API
#
# 如果你想使用官方套件（可選）：
# yfinance>=0.2.40      # 美股數據（更穩定但可能需要處理速率限制）
# akshare>=1.13.0       # A股數據（更多功能但體積較大）

# 可選：開發工具
# pytest>=8.0.0         # 用於單元測試
# black>=24.0.0         # 用於程式碼格式化
# ruff>=0.3.0           # 用於程式碼檢查
